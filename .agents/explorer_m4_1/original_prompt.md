## 2026-06-10T20:07:03Z
Read PROJECT.md, SCOPE.md, and ORIGINAL_REQUEST.md.
Investigate the current Next.js project layout and `app/page.tsx`.
Our goal is to implement the React/Next.js UI for Milestone 4 (Hero, Method, Portfolio, Reviews, Double Funnel).
CRITICAL: You must use `framer-motion` for all animations (scroll reveals, page transitions, hover states). The design style is 'editoriale', warm neutrals (crema, beige, panna), gold/bronze accents. Elegant serif for titles, clean sans-serif for paragraphs. Asymmetrical gallery for the portfolio.
The Double Funnel must use the existing `saveLead` server action in `app/actions/saveLead.ts` and embed a Calendly widget.
Produce a step-by-step strategy for the Worker to rewrite `app/page.tsx` and create any necessary components in `components/`.
You do NOT implement the code. Just provide the architectural layout and detailed instructions for the UI sections.
Write your findings to a handoff report.
